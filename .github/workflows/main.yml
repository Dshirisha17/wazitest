name: SSH Connection

on:
  workflow_dispatch:

jobs:
  ssh-job:
    runs-on: self-hosted

    steps:
    - name: Run ls -lrt on Remote Server using Password Authentication
      run: |
        echo sandhata123 | ssh -o StrictHostKeyChecking=no -p 2222 ibmsys1@10.10.1.180 "ls -lrt"
      shell: cmd
