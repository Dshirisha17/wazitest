name: SSH Connection

on:
  workflow_dispatch:

jobs:
  ssh-job:
    runs-on: self-hosted

    steps:
    - name: Run ls -lrt on Remote Server using Plink
      run: |
        plink -batch -P 2222 -pw s3cure ibmuser@10.10.1.180 "ls -lrt"
      shell: cmd
