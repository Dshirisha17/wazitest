name: SSH Login with password

on:
  workflow_dispatch:

jobs:
  ssh-login:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Execute Command on Linux Machine
        run: |
          plink -ssh -pw "s3cure" ibmuser@10.10.1.180 -p 2222 "echo 'Logged in successfully' && ls -lrt"
