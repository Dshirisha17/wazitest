name: SSH Connection

on:
  workflow_dispatch:

jobs:
  ssh-job:
    runs-on: self-hosted

    steps:
    - name: Run ls -lrt on Remote Server using Password Authentication
      run: |
        echo s3cure | ssh -o StrictHostKeyChecking=no -p 2222 ibmuser@10.10.1.180 "ls -lrt"
      shell: cmd
